# AI Creative Studio - Backend

Run locally:

```
cp .env.example .env
npm run dev
```

Required env:

```
PORT=4000
MONGO_URI=your_mongodb_atlas_uri
JWT_SECRET=your_secret
MOCK_MODE=true
USE_MEMORY_DB=false
FRONTEND_URL=http://localhost:5173
```

Deploy:

- Render/Heroku. Set env vars as above.
